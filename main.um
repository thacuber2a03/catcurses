import (
	cc = "catcurses.um"
)

const msg = "hello world!"

fn main() {
	term := cc::stdTerminal()
	term.raw(true)
	term.echo(false)

	win := term.window
	w, h := win.getSize()
	win.printAt(w/2, (h-len(msg))/2, msg)
	win.refresh()
	win.getKey()

	term.destroy()
}
